# Postgres.app with trust auth (see README PostgreSQL section) needs no
# password locally; adjust user/host/port for a different setup.
DATABASE_URL=postgresql+psycopg2://your_username@localhost:5432/ipl_intelligence
CRICKET_API_KEY=xxxx
